import SwiftUI

struct SearchView: View {
    var body: some View {
        ScrollView {
            Text("Search")
        }
    }
}

struct TestView: View {
    var body: some View {
        Text("Hello")
    }
}
